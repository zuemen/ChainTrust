# 主要相依一律「下界 + 上界」：下界＝實際用到的 API 出現版本，上界＝下一個 major
# （避免 major bump 靜默破壞已 commit 的 model.joblib 與訓練/推論行為）。
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
lightgbm>=4.3,<5.0
# train.py 使用 sklearn.frozen.FrozenEstimator（取代舊 cv="prefit"），該模組自 1.6 才存在。
# 先前寫 >=1.4 會在 1.4/1.5 上 ImportError。
scikit-learn>=1.6,<2.0
pandas>=2.2,<4.0
numpy>=1.26,<3.0
joblib>=1.4,<2.0
pytest>=8.0,<10.0
httpx>=0.27,<1.0
